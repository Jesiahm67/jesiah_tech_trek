<nav class="bg-blue-400 p-4 shadow-lg border-b-4 border-blue-600">
  <div class="max-w-7xl mx-auto flex items-center justify-between">
    
    <!-- Logo -->
    <a class="text-xl md:text-2xl font-bold tracking-tight" href="/">Techbar</a>

    <!-- Hamburger Menu Button (Visible only on mobile) -->
    <button id="menu-toggle" class="md:hidden flex flex-col gap-1.5 focus:outline-none">
      <div class="w-6 h-1 bg-black rounded"></div>
      <div class="w-6 h-1 bg-black rounded"></div>
      <div class="w-6 h-1 bg-black rounded"></div>
    </button>

    <!-- Navigation Links -->
    <!-- On mobile: hidden by default, absolute positioning when toggled -->
    <!-- On desktop (md): flex-row, relative positioning -->
    <div id="nav-links" class="hidden absolute top-16 left-0 w-full bg-blue-400 flex-col items-center gap-4 p-6 shadow-xl z-50 md:static md:flex md:flex-row md:w-auto md:p-0 md:shadow-none md:top-0">

        {% if not current_user.is_authenticated %}
            <a href="/signup" class="button-link w-full text-center md:w-auto">Sign Up</a>   
        {% elif current_user.is_authenticated %}
            <a href="/browse" class="hover:underline font-semibold" style="color: indigo;">Browse</a>
            <a href="/contact" class="hover:underline font-semibold" style="color: maroon;">Contacts</a>
            <a href="/sales" class="hover:underline font-semibold" style="color: darkgoldenrod;">Sales</a>
            <a href="/wishlist" class="hover:underline font-semibold" style="color: darkgreen;">Wishlist</a>
            <a href="/logout" class="button-link w-full text-center md:w-auto">Logout</a>
            
            <span class="text-sm font-bold bg-white/20 px-3 py-1 rounded-full">
                Hi, {{ current_user.name }}!
            </span>
        {% endif %}

    </div>
  </div>
</nav>

<!-- Minimal JS to toggle the mobile menu -->
<script>
  const btn = document.getElementById('menu-toggle');
  const menu = document.getElementById('nav-links');

  btn.addEventListener('click', () => {
    menu.classList.toggle('hidden');
    menu.classList.toggle('flex');
  });
</script>

<style>
.button-link {
    display: inline-block;
    padding: 8px 20px;
    background-color: #dfabc2;
    color: #000;
    text-decoration: none;
    border-radius: 10px;
    font-weight: bold;
    transition: all 0.2s ease;
}
.button-link:hover {
    background-color: orangered;
    color: white;
    transform: translateY(-1px);
}
</style>
